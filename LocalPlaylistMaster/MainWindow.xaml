<Window x:Class="LocalPlaylistMaster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LocalPlaylistMaster"
        mc:Ignorable="d"
        Title="Local Playlist Master" Height="450" Width="800">

    <Grid>
        <DockPanel LastChildFill = "True">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_New" Click="CreateNewPlaylist"/>
                    <MenuItem Header="_Open" />
                    <MenuItem Header="_Save" />
                    <Separator />
                    <MenuItem Header="_Exit" />
                </MenuItem>
                <MenuItem Header="_Import">
                    
                </MenuItem>
                <MenuItem Header="_Export"></MenuItem>
            </Menu>
            <StackPanel x:Name="Bottom_Bar" DockPanel.Dock = "Bottom" Orientation = "Horizontal" Height="20" Background="DimGray">
                <Image Source="{Binding StatusImage}"></Image><TextBlock Text="{Binding StatusText}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" DockPanel.Dock = "Right" MinWidth="250" Background="Gray">

            </StackPanel>
            <ScrollViewer DockPanel.Dock="Left" MinWidth="100">
                <ListBox ItemsSource="{Binding playlistManager}" Grid.IsSharedSizeScope="True" HorizontalContentAlignment="Stretch" IsSynchronizedWithCurrentItem="True">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Key" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"  />
                                <TextBox Grid.Column="1" Text="{Binding Value }" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </DockPanel>
    </Grid>


</Window>
